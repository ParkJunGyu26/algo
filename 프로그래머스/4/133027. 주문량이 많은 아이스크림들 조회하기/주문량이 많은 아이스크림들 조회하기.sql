-- 코드를 입력하세요

SELECT uni.FLAVOR
FROM
(
    SELECT FLAVOR, TOTAL_ORDER
    FROM FIRST_HALF

    UNION

    SELECT FLAVOR, TOTAL_ORDER
    FROM JULY
) as uni
GROUP BY uni.FLAVOR
ORDER BY SUM(uni.TOTAL_ORDER) DESC
LIMIT 3